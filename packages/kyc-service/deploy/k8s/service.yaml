apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  labels:
    app.kubernetes.io/name: kyc-service
spec:
  selector:
    app.kubernetes.io/name: kyc-service
  ports:
    - name: http
      port: 80
      targetPort: http
