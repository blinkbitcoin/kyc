# Refuse installs on Node versions outside package.json "engines"
# (repo toolchain is pinned to node 24 via flake.nix; engines floor is 22.22 / 24.15, set by jsdom 30)
engine-strict=true

# Quieter installs
fund=false
